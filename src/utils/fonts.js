import { Playfair_Display, Jost } from "next/font/google";

export const PlayfairDisplayFont = Playfair_Display({ subsets: ["latin"], variable: "--font-playfair-display" });

export const JostFont = Jost({ subsets: ["latin"], variable: "--font-jost" });